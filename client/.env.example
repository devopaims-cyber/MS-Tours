# Vite reads VITE_* vars from this file (not the root .env).
# Keep in sync with the server's PORT in ../.env.

# Development
VITE_API_URL=http://localhost:5001/api

# Production build example:
# VITE_API_URL=https://api.your-domain.com/api
